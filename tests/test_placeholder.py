"""Placeholder test to ensure pytest runs."""


def test_placeholder() -> None:
    """Placeholder test that always passes.

    This ensures the test suite runs during CI/CD
    until real tests are added.
    """
    assert True
